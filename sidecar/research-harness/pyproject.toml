[project]
name = "research-harness"
version = "0.1.0"
description = "ResearchOps harness for multi-scenario, multi-channel desk research."
requires-python = ">=3.10"
dependencies = []

[project.scripts]
research = "research_harness.cli:main"

[tool.setuptools]
packages = ["research_harness"]

