/// <reference types="vite/client" />

// Injected by vite.config.ts `define` at build time: the git-derived version
// (e.g. "0.1.5-6-g13bda02"). Empty string when git is unavailable.
declare const __GIT_APP_VERSION__: string;
